package ru.wert.bazapik_mobile.chat.websocket;

//import ua.naiksoftware.stomp.Stomp;
//import ua.naiksoftware.stomp.StompClient;

import static ru.wert.bazapik_mobile.ThisApplication.DATA_BASE_URL;

public class ChatConnection {

    private static final String TAG = "ChatConnection";

//    private StompClient mStompClient;

    public ChatConnection() {
//        mStompClient = Stomp.over(Stomp.ConnectionProvider.OKHTTP, "ws://" + DATA_BASE_URL + "/example-endpoint/websocket");
    }
}
